// Prevents additional console window on Windows in release
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

mod ai;
mod commands;
mod content_chunking;
mod db;
mod parsers;
mod search;
mod semantic;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_store::Builder::default().build())
        .plugin(tauri_plugin_dialog::init())
        .invoke_handler(tauri::generate_handler![
            commands::config::get_config,
            commands::config::set_config,
            commands::config::fetch_models,
            commands::config::import_commentator_from_skill,
            commands::config::get_profiles,
            commands::config::set_profiles,
            commands::extract::plan_content,
            commands::extract::parse_document,
            commands::extract::get_file_metadata,
            commands::extract::upsert_source_document,
            commands::extract::extract_text,
            commands::extract::extract_text_streaming,
            commands::extract::fetch_url,
            commands::extract::describe_image,
            commands::extract::analyze_text_streaming,
            commands::extract::analyze_text_block,
            commands::extract::fact_check_claim,
            commands::library::save_points,
            commands::library::save_manual_point,
            commands::library::save_fact_check_point,
            commands::library::save_evidence,
            commands::library::list_evidence_for_point,
            commands::library::list_evidence_for_source,
            commands::library::get_source_assets,
            commands::library::list_recent_evidence,
            commands::library::get_evidence,
            commands::library::search_evidence,
            commands::library::search_assets,
            commands::library::explain_search_ranking,
            commands::library::build_block_reference_manifest,
            commands::library::build_board_snapshot_export,
            commands::library::build_retrieval_context,
            commands::library::suggest_backlinks,
            commands::library::save_asset_search,
            commands::library::list_saved_asset_searches,
            commands::library::preview_saved_asset_search,
            commands::library::delete_saved_asset_search,
            commands::library::save_retrieval_profile,
            commands::library::list_retrieval_profiles,
            commands::library::preview_retrieval_profile,
            commands::library::delete_retrieval_profile,
            commands::library::save_quick_capture,
            commands::library::list_quick_captures,
            commands::library::resolve_quick_capture,
            commands::library::dismiss_quick_capture,
            commands::library::list_report_starter_templates,
            commands::library::list_command_palette_items,
            commands::library::build_capability_scorecard,
            commands::library::load_automation_suggestions,
            commands::library::load_import_diagnostics_ledger,
            commands::library::build_report_starter,
            commands::library::load_reprocess_queue,
            commands::library::detect_duplicate_assets,
            commands::library::build_graph_neighborhood_preview,
            commands::library::save_report,
            commands::library::list_recent_reports,
            commands::library::get_report,
            commands::library::search_reports,
            commands::library::locate_citation_quote,
            commands::library::load_report_citation_audit,
            commands::library::load_report_invocation_audit,
            commands::library::load_report_audit,
            commands::library::load_citation_quality_dashboard,
            commands::library::run_investigation_qa_eval,
            commands::library::delete_report,
            commands::library::save_journal_entry,
            commands::library::list_recent_journal_entries,
            commands::library::search_journal_entries,
            commands::library::invalidate_journal_entry,
            commands::library::discover_related_assets,
            commands::library::rebuild_asset_relations,
            commands::library::add_review_item,
            commands::library::list_due_review_items,
            commands::library::list_all_review_items,
            commands::library::build_review_queue_plan,
            commands::library::complete_review_item,
            commands::library::snooze_review_item,
            commands::library::dismiss_review_item,
            commands::library::get_open_data_mirror_config,
            commands::library::set_open_data_mirror_config,
            commands::library::build_open_data_mirror_plan,
            commands::library::build_export_sync_audit,
            commands::library::export_open_data_mirror,
            commands::library::load_open_data_mirror_manifest,
            commands::library::prune_open_data_mirror,
            commands::library::add_indexed_folder,
            commands::library::list_indexed_folders,
            commands::library::scan_indexed_folder,
            commands::library::list_indexed_files_for_folder,
            commands::library::load_indexed_file_preview,
            commands::library::remove_indexed_folder,
            commands::library::list_points,
            commands::library::search_points,
            commands::library::search_workspace,
            commands::library::get_point_source_context,
            commands::library::open_source_workspace,
            commands::library::list_recent_sources,
            commands::library::get_source_workspace_summary,
            commands::library::delete_point,
            commands::library::archive_point,
            commands::library::unarchive_point,
            commands::library::list_archived_points,
            commands::library::star_point,
            commands::library::unstar_point,
            commands::library::get_starred_count,
            commands::library::list_starred_points,
            commands::explore::list_mental_models,
            commands::explore::recommend_frameworks,
            commands::explore::deepen_point,
            commands::explore::polish_manual_thought,
            commands::explore::find_similar,
            commands::explore::classify_related,
            commands::digest::generate_digest,
            commands::digest::generate_synthesis,
            commands::digest::generate_investigation,
            commands::gallery::generate_image,
            commands::gallery::prepare_gallery_image_prompt,
            commands::gallery::generate_image_from_prompt,
            commands::gallery::list_gallery,
            commands::gallery::search_gallery,
            commands::gallery::delete_gallery_item,
            commands::gallery::retry_download,
            commands::gallery::diagnose_gallery_file,
            commands::analytics::get_analytics,
            commands::analytics::get_explore_suggestions,
            commands::suggestions::generate_suggestion,
            commands::suggestions::save_suggestion,
            commands::suggestions::list_suggestions_by_date,
            commands::suggestions::get_suggestion,
            commands::suggestions::delete_suggestion,
            commands::suggestions::list_marked_dates,
            semantic::commands::get_semantic_index_status,
            semantic::commands::rebuild_semantic_index,
            semantic::commands::cancel_semantic_index_rebuild,
            semantic::commands::hybrid_semantic_search,
            semantic::commands::generate_grounded_answer,
            semantic::commands::save_grounded_answer_report,
            semantic::commands::check_database_integrity,
            semantic::commands::backup_database,
            semantic::commands::restore_database_backup,
            semantic::commands::store_semantic_api_key,
            semantic::commands::semantic_api_key_status,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
